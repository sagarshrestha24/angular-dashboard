name: Angular Dashboard

on:
  push:
    branches:
      - master

jobs:
  
  testangularapplication:
    permissions:
      contents: read
      packages: write
    runs-on: ubuntu-latest

    steps:
    - name: Check out my other private repo
      uses: actions/checkout@master
          
    - name: test
      run: |
       git clone https://sagarshrestha24:${{ secrets.GITHUB_TOKEN }}@github.com/sagarshrestha24/cortex-promethes-script.git
      
    
    
        
  

         
